import styled from 'styled-components'

export const Button = styled.button`
  border: none;
  background-color: transparent;
  font-size: 1em;
  font-family: inherit;

  &:active {
    outline: none;
  }
`